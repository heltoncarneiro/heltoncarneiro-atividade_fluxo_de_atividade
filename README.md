# API de Filmes

API REST simples usando **Node.js + Express**.  
Nesta versão inicial temos apenas a rota **GET**.

## Como executar
```bash
npm install
node index.js
```

## Endpoint
- **GET** http://localhost:5000/api/filmes → retorna a lista de filmes